version: 0.2

phases:
  build:
    commands:
      - make test-artifacts-in-docker
  post_build:
    commands:
      - cp out/test-artifacts/agent.exe .
artifacts:
  files:
      - 'agent.exe'
      - 'misc/windows-deploy/hostsetup.ps1'
      - 'out/**/*'
      - 'agent/functional_tests/testdata/**/*'
  discard-paths: no
